# Examples

The primary example is the checked-in local fixture used by `evaluate.py`.
It demonstrates privacy-conscious identity behavior analytics, anomaly scoring, risk explanations, and recommendation-only response without requiring external accounts or
credentials. Add new examples with expected outcomes and a short explanation.
